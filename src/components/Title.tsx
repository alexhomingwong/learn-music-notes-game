import React from "react";
import styled from "styled-components";

const StyledTitle = styled.div`
  text-align: center;
  font-size: 3rem;
  margin-bottom: 1rem;
`;

export const Title = () => {
  return <StyledTitle>How well can you read music?</StyledTitle>;
};
